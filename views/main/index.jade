extends ../layout

block title
	title ГЛАВНАЯ

block content_style
	link(rel='stylesheet', type='text/css', href='/build/css/blocks_grid.css' + version)
	link(rel='stylesheet', type='text/css', href='/build/css/main.css' + version)
	link(rel='stylesheet', type='text/css', href='/build/libs/css/jquery.fancybox.css' + version)
block content_scripts
	script(type='text/javascript', src='/build/libs/js/imagesloaded.pkgd.min.js' + version)
	script(type='text/javascript', src='/build/libs/js/masonry.pkgd.min.js' + version)
	script(type='text/javascript', src='/build/libs/js/jquery.fancybox.js' + version)
	//script(type='text/javascript', src='/build/js/scroll.js' + version)
	script(type='text/javascript', src='/build/js/main.js' + version)
	script(type='text/javascript', src='/build/js/fancybox.js' + version)


block content
	- var months = ['января', 'февраля', 'мара', 'апреля', 'мая', 'июня', 'июля', 'августа', 'сентября', 'октября', 'ноября', 'декабря']
	- ev_lnght = Math.floor(Math.random() * ((events.length - 1) - 0 + 1)) + 0

	.screen_block.intro_block(style='background-image:url(' + events[ev_lnght].images[0].original + ')')
		.intro_block_inner
			img(src='/images/design/logo.png')
			| Арсений Россихин
	.content_navigate_block
		.navigate_title Наши последние события:
		.navigate_items_block
			.navigate_item.excursion экскурсии
			.navigate_item.exhibition выставки
			.navigate_item.lecture лекции
			.navigate_item.action мероприятия
			.navigate_item.xxi Дискуссионная площадка Тверская XXI
	.content_outer_block


		each event in events
			if (event.status == 'important')
				.content_item.event.important(href='/events/' + event.type +'/' + event._id, style='background-image: url(' + (event.images && event.images.length > 0 && event.images[0].original) + ')')
					.item_description
						.item_date= ('0' + event.interval.start.getUTCDate()).slice(-2)  + '/' + ('0' + event.interval.start.getUTCMonth()).slice(-2) + '/' + ('0' + event.interval.start.getUTCFullYear()).slice(-2) + ' - ' + ('0' + event.interval.end.getUTCDate()).slice(-2)  + '/' + ('0' + event.interval.end.getUTCMonth()).slice(-2) + '/' + ('0' + event.interval.end.getUTCFullYear()).slice(-2)
					.item_title= event.i18n.title.get('ru')
						each image in event.images
							a.item_fancy(href=image.original, data-fancybox-group='' + event._id + '')

			else
				.content_item.event(href='/events/' + event.type +'/' + event._id)
					img.item_image(src=event.images && event.images.length > 0 && event.images[0].thumb)
					.item_description
						.item_date= ('0' + event.interval.start.getUTCDate()).slice(-2)  + '/' + ('0' + event.interval.start.getUTCMonth()).slice(-2) + '/' + ('0' + event.interval.start.getUTCFullYear()).slice(-2) + ' - ' + ('0' + event.interval.end.getUTCDate()).slice(-2)  + '/' + ('0' + event.interval.end.getUTCMonth()).slice(-2) + '/' + ('0' + event.interval.end.getUTCFullYear()).slice(-2)
						.item_title= event.i18n.title.get('ru')
							each image in event.images
								a.item_fancy(href=image.original)